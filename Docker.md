# DOCKER COMMAND

## DOCKER COMPOSE

1. Build docker-compose
   `docker-compose build name_build`

2. Up docker-compose
   `docker-compose up name`

## DOCKER IMAGE

1. Get all image
   ` docker image ls`

## DOCKER CONTAINER

1. Get all container
   `docker ps -a`
